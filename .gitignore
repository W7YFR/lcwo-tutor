# ---- your practice data: never commit ----
# The database is the record of every session you have run.
lcwo.db
*.db
# SQLite sidecars, present while a connection is open or after a crash
*.db-journal
*.db-wal
*.db-shm
*.sqlite
*.sqlite3

# Generated reports embed the whole graded dataset as JSON, so publishing one
# publishes everything lcwo.db holds. Rebuild any time with `make report`.
reports/
exports/
# catches `lcwo.py report -o somewhere.html` landing in the tree
*.html
# ...except the browser extension, which is source
!lcwo-tools/src/ext/*.html
# ...and the saved LCWO markup the tests parse. These are hand-written and
# carry invented groups; the real captures in lcwo-html/ stay ignored.
!lcwo-tools/test/fixtures/*.html

# built extension zips - `make build` remakes them from source
lcwo-tools/dist/

*.bak

import_history.py

# ---- Python ----
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.mypy_cache/
.pytest_cache/
.ruff_cache/

# ---- editors / OS ----
.DS_Store
.idea/
.vscode/
*.swp
